import layout from "./layout";
import { moderateScale } from "./scale";

export default {
  header: {
    height: moderateScale(layout.height * 0.05),
  },
  labels: {
    height: moderateScale(layout.height * 0.05),
  },
};
